<div class="container">
  <div class="cards-grid">
        <% @dolls.each do |doll| %>
      <%= link_to doll_path(doll.id) do %>
        <div class="doll-card">
          <%= cl_image_tag doll.photo.key %>

        </div>
      <% end %>
    <% end %>
  </div>
</div>
